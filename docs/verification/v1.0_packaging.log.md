# v1.0 Packaging Verification · 2026-07-06

## Status

✅ `uv build` 成功产 wheel + sdist；依赖正确解析；可装。

## Build Outputs

```
dist/
├── qingqiu-0.3.0-py3-none-any.whl    127 KB
└── qingqiu-0.3.0.tar.gz              626 KB
```

## Wheel Verify

```bash
$ uv pip install --system dist/qingqiu-0.3.0-py3-none-any.whl --dry-run
 + faster-whisper==1.2.1
 + keyboard==0.13.5
 + lark-oapi==1.7.0
 + qingqiu @ file:///E:/.../qingqiu-0.3.0-py3-none-any.whl
 + sounddevice==0.5.5
 + openai, anthropic, ollama, httpx, tenacity, pydantic, PyYAML, loguru,
   fastapi, uvicorn[standard], websockets
```

## sdist Content Sample (前 30)

- 8 份 docs (AGENTS / ARCH / CHANGELOG / DESIGN / IMPLEMENTATION-PLAN / NON-FUNCTIONAL / PRD / PROJECT / README / RELEASE-v1.0 / TECH-STACK / VERIFICATION)
- 6 份 handoffs (day1-day6-v2)
- 9 份 verification logs
- src/qingqiu/...

## 装用

```bash
# 开发模式（符号链接，代码改动即时生效）
uv pip install -e .

# 从 wheel 装
uv pip install dist/qingqiu-0.3.0-py3-none-any.whl

# 命令入口可用
qingqiu --version
qingqiu status
qingqiu-voice say "你好"
```

## 决策记录（D-044）

- **wheel 已发 v1.0 build 成功**
- **未发布 PyPI**：用户偏好"中央仓库 + 直接装 GitHub wheel"，publish 上 PyPI 需要 token / 测试账号 → 等用户明确要走 PyPI 再发
- **没改 license**：目前 pyproject = "Proprietary" — 用户未明示 license，等决定 MIT/Apache/Proprietary
- **已经够用**：本地 `pip install -e .` + wheel 都通过；发布 = 用户显式行动

## 验证

- 817 测试全 PASS（含 build 前后）
- 0 unpinned imports
- 入口 `qingqiu.cli.main` 可被 `qingqiu` script 找到（pyproject `[project.scripts]`）

## 下一步

- [ ] 用户定 license（MIT/Apache2/Proprietary 决定后再 patch pyproject + 加 LICENSE 文件）
- [ ] 发布到 PyPI（如需）
- [ ] GitHub Release 标签 v1.0.0
# S1.2 真跑日志：Custom provider · MiniMax API

> **切片：** S1.2 LLM 抽象层
> **日期：** 2026-07-05
> **目的：** 验证 CustomProvider 真能调通 MiniMax API（不是 mock）

## 环境

- Python: 3.12.11
- 平台: Windows 11
- Qingqiu: 0.3.0
- Provider: Custom (MiniMax API · OpenAI 兼容)
- API endpoint: `https://api.MiniMax.chat/v1`
- Model: `MiniMax-Text-01`

## 安全处理

- ✅ API key 仅设到当前 PowerShell session 环境变量
- ✅ **未写入任何文件**（不写 config.yaml / .env / 本文件）
- ✅ 跑完立即清除环境变量（`$env:CUSTOM_LLM_API_KEY = $null`）
- ✅ 本日志**不记 key**

## 命令

```powershell
$env:CUSTOM_LLM_URL = "https://api.MiniMax.chat/v1"
$env:CUSTOM_LLM_API_KEY = "<key · 已脱敏 · 不在本文件>"
$env:CUSTOM_LLM_MODEL = "MiniMax-Text-01"
uv run qingqiu llm test custom
```

## 输出（完整）

```
[test] 初始化 custom provider ...
[test] 初始化 OK (default_model=MiniMax-Text-01)
[test] 发送测试 prompt ...
[OK] custom 响应：
  content: 'Hello from custom!'
  model:   MiniMax-Text-01
  usage:   input=562, output=4
```

## 响应分析

| 字段 | 值 | 含义 |
|------|----|----|
| content | `Hello from custom!` | ✅ 真实 API 返回内容（不是 mock 的预置字符串） |
| model | `MiniMax-Text-01` | ✅ 真实模型名（MiniMax 确认响应） |
| input | 562 | 系统 prompt "Say hello from custom and nothing else." + 一些 token 化开销 |
| output | 4 | "Hello from custom!" ≈ 4 tokens |

## 响应时长

未精确测量（VERIFICATION.md v1.0 未要求记录），体感 < 2s

## 结论

✅ **S1.2 真跑落地 PASS**

- ✅ 真实 HTTP 请求发到 `https://api.MiniMax.chat/v1`
- ✅ 真实响应（content 是 MiniMax 生成，不是 mock）
- ✅ 真实 token 计量（input=562 / output=4 来自 MiniMax）
- ✅ provider 接口工作正常（init + complete + response parse 全流程）

## 4 个 provider 状态

| Provider | mock 测试 | 真跑验证 | 状态 |
|----------|----------|---------|------|
| OpenAI | ✅ 6 测试 | ⚪ 未真跑 | mock 通过，待真跑 |
| Anthropic | ✅ 7 测试 | ⚪ 未真跑 | mock 通过，待真跑 |
| Ollama | ✅ 6 测试 | ⚪ 未真跑 | mock 通过，待真跑（需本地 Ollama） |
| **Custom (MiniMax)** | ✅ 5 测试 | **✅ 真跑 PASS** | **已真跑落地** |

## 下一步

- S1.2 切片**正式完成**（不只是 mock 通过）
- 其他 3 个 provider 等用户方便时真跑（不需要 4 个都跑，至少 1 个真跑通就证明抽象层工作）
- 进入 S1.3 配置系统（YAML + 优先级 + 热重载）

🤖 Generated with Claude Code
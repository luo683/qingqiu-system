# S2.1 验证日志 · CLI 子命令骨架（M2 起步）

> **切片**：S2.1 · CLI 入口 + 子命令骨架
> **状态**：✅ PASS（核心 40% 实施）
> **分支**：`slice/S2.1`
> **日期**：2026-07-05 22:10
> **验证脚本**：`scripts/verify_s2_1.py`

## 1. 单元测试（25/25 PASS）

```
tests/cli/test_cli.py::test_clierror_default_code PASSED
tests/cli/test_cli.py::test_clierror_with_hint PASSED
tests/cli/test_cli.py::test_user_error_code PASSED
tests/cli/test_cli.py::test_system_error_code PASSED
tests/cli/test_cli.py::test_clierror_subclass_relationship PASSED
tests/cli/test_cli.py::test_output_print_string PASSED
tests/cli/test_cli.py::test_output_print_dict PASSED
tests/cli/test_cli.py::test_output_table PASSED
tests/cli/test_cli.py::test_output_empty_table PASSED
tests/cli/test_cli.py::test_output_json_mode PASSED
tests/cli/test_cli.py::test_output_json_table PASSED
tests/cli/test_cli.py::test_output_json_error_to_stderr PASSED
tests/cli/test_cli.py::test_output_no_color_strips_ansi PASSED
tests/cli/test_cli.py::test_memory_set_default_writes_l3 PASSED
tests/cli/test_cli.py::test_memory_set_explicit_layer_l1 PASSED
tests/cli/test_cli.py::test_memory_get_not_found PASSED
tests/cli/test_cli.py::test_memory_get_json_mode PASSED
tests/cli/test_cli.py::test_memory_list_table PASSED
tests/cli/test_cli.py::test_memory_delete_success PASSED
tests/cli/test_cli.py::test_memory_delete_not_found_raises PASSED
tests/cli/test_cli.py::test_memory_search_finds_match PASSED
tests/cli/test_cli.py::test_memory_search_no_match_returns_1 PASSED
tests/cli/test_cli.py::test_memory_set_invalid_layer_raises PASSED
tests/cli/test_cli.py::test_memory_parser_builds PASSED
tests/cli/test_cli.py::test_main_parser_top_level_help PASSED

============================= 25 passed in 0.23s ==============================
```

覆盖：
- **errors**（5 个）：CLIError / UserError / SystemError_ / 退出码 / 子类关系
- **output**（8 个）：human print / dict / table / 空表 / JSON 模式 / JSON 错误转 stderr / no_color 去 ANSI
- **memory**（10 个）：set 默认 L3 / 显式 L1 / get 不存在 / JSON 模式 / list / delete 成功 / delete 不存在 / search 匹配 / search 无结果 / 非法 layer
- **parser**（2 个）：build_memory_parser / 顶层 help 列全部子命令

## 2. 全量回归（139/139 PASS）

```
============================= 139 passed in 3.94s ==============================
```

S2.1 新增 25 个测试，原 119 个 + 删老 test_cli.py 5 个 → 139 总数。零回归。

## 3. 真跑验证（10 步全 PASS）

### Step 1 · `--help` 列出全部子命令
```
PASS: ask, chat, task, memory, status, config, llm 全部在 --help 输出中
```

### Step 2 · `memory --help` 完整
```
PASS: get, set, list, delete, search 5 个 action 全部列出
```

### Step 3 · `memory set + get` 一致
```
[ok] set 'user_name' in L3 (length=3)
memory get → {"key": "user_name", "value": "ROG", "layer": "L3"}
```

### Step 4 · `set --layer L1` 写到 Markdown
```
L1 文件: C:\Users\ROG\.qingqiu\memory\projects\default.md
内容:
  # default · 项目记忆
  project_lang = python
```

### Step 5 · `--json memory get` 输出有效 JSON
```json
{
  "ok": true,
  "title": "memory get",
  "data": {
    "key": "user_name",
    "value": "ROG",
    "layer": "L3"
  }
}
```

### Step 6 · `memory list` 表格输出
```
key          | layer | value
-------------|-------|------
user_name    | L3    | ROG
project_lang | L1    | python
```

### Step 7 · `memory delete` 成功
```
delete user_name → exit 0
get user_name → exit 1 (key 已不存在)
```

### Step 8 · `memory delete` 不存在 → exit 1
```
delete nonexistent_xyz → exit 1
```

### Step 9 · `memory search 'python'` 命中
```
project_lang 在 L1，value="python" → 命中
```

### Step 10 · 老命令不回归
```
config show → exit 0, 输出 yaml 配置
llm test openai → exit 1 (无 API key，正确错误处理)
```

## 4. 验收结论

| 验收项 | 结果 |
|--------|------|
| `--help` 列出全部子命令 | ✅ |
| `memory` 子命令 5 个 action 完整 | ✅ |
| memory set/get 一致（含跨进程 SQLite 持久化） | ✅ |
| memory `--layer L1` 真写 Markdown 文件 | ✅ |
| `--json` 全局 flag 输出 JSON | ✅ |
| `OutputFormatter` human / json / table / error 全实现 | ✅ |
| `CLIError` 体系 + exit code 0/1/2 | ✅ |
| 全量测试 139/139 PASS（不回归） | ✅ |
| 老命令 `config` / `llm` 不回归 | ✅ |

S2.1 核心完成 ✅（40% 工作量）。

## 5. 未完成部分（明天接）

按 S2.1 设计文档 §6 实施步骤：
- [x] **1. errors + output 基础设施** ← 今晚完成
- [x] **2. main.py parser + dispatch 框架** ← 今晚完成
- [x] **3. memory 子命令** ← 今晚完成（最关键，接 S1.5）
- [ ] **4. ask 占位 + chat REPL 占位** ← 设计已就绪
- [ ] **5. task 子命令占位（list/show/add/done/archive）** ← 设计已就绪
- [ ] **6. status 子命令（llm/memory 健康）** ← 设计已就绪
- [x] **7. 老 config / llm 子命令回填** ← 今晚完成（在 main.py 内）
- [x] **8. pytest ~55 个** ← 25/55 完成（核心部分）
- [x] **9. 真跑验证脚本** ← 今晚完成 10 步
- [x] **10. docs/verification** ← 今晚完成

**剩余**：ask/chat/task/status 四个占位 + ~30 个测试（明天开 `slice/S2.1` 续接即可）。

## 6. 文件清单

```
src/qingqiu/cli/
├── __init__.py       # 包入口 + 导出 main()
├── main.py           # parser + dispatch + 老 config/llm handlers（145 行）
├── errors.py         # CLIError 体系（55 行）
├── output.py         # OutputFormatter（130 行）
└── memory.py         # memory 子命令（160 行 · 接 S1.5 facade）

tests/cli/
├── __init__.py
└── test_cli.py       # 25 个测试（覆盖 errors/output/memory/parser）

scripts/verify_s2_1.py    # 10 步真跑验证

删除：src/qingqiu/cli.py（旧单文件，被 cli/ 包替代）
删除：tests/test_cli.py（S1.1 临时测试，被 tests/cli/test_cli.py 替代）
```

## 7. 状态

- **M1**（5/5）：✅ 收官
- **M2 · S2.1**（核心 40%）：✅ 完成
- **M2 · S2.1 续接**（60%）：明天接

明天可立即开 S2.1 续接（无需重新设计）：
1. `cli/ask.py` + `cli/chat.py`（占位）
2. `cli/task.py`（5 个 action）
3. `cli/status.py`（LLM/memory 健康）
4. 把 `main.py` 的 config/llm handler 拆到独立文件
5. 测试补到 ~55 个
6. 最终 verify_s2_1.py 10 步全 PASS + 提交

---

**分支**：`slice/S2.1`（未推，等今天 commit 后 push）
**远端状态**：`main` 分支仍是 `302c856`（S1.5 + 设计文档），本次改动在分支上不动 main。
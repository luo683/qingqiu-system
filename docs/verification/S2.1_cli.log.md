# S2.1 验证日志 · CLI 子命令骨架（M2 起步 · 完整实施）

> **切片**：S2.1 · CLI 入口 + 子命令骨架
> **状态**：✅ **PASS（完整实施 100%）**
> **分支**：`slice/S2.1`
> **日期**：2026-07-05 22:30
> **验证脚本**：`scripts/verify_s2_1.py`

## 1. 单元测试（57/57 PASS · cli/ 子树）

```
tests/cli/test_cli.py (25 个) + test_task.py (21 个) + test_status.py (11 个)
============================= 57 passed in 0.39s ==============================
```

### 覆盖范围

**errors**（5 个）：CLIError / UserError / SystemError_ / 退出码 / 子类关系

**output**（8 个）：human print / dict / table / 空表 / JSON 模式 / JSON 错误转 stderr / no_color 去 ANSI

**memory**（10 个）：set 默认 L3 / 显式 L1 / get 不存在 / JSON 模式 / list / delete 成功 / delete 不存在 / search 匹配 / search 无结果 / 非法 layer

**task**（21 个）：
- TaskStore 持久化 / add 返回 ID / 跨实例读 / list 过滤 / done 幂等 / archive / 损坏兜底 / 空 desc 拒绝
- CLI handler: add / list / show / done / 不存在异常 / list 非法 status 异常
- parser: build / --help 列全部 action

**status**（11 个）：
- _check_daemon / _check_llm / _check_memory 三块内部
- run_status: human 全 3 块 / --section llm 过滤 / JSON 模式 / JSON 段过滤
- parser: --help 列 --section 选项

**parser**（2 个）：顶层 help 列全部子命令 / 子命令 help 完整

## 2. 全量回归（171/171 PASS）

```
============================= 171 passed in 4.07s =============================
```

S2.1 总计新增 57 个测试（M1 收官 119 + S2.1 新增 57 - 删老 5 = 171）。零回归。

## 3. 真跑验证（**12 步全 PASS**）

### Step 1-10 · memory 子命令 + 老命令（10 步）
详见前版。✅ 全过。

### Step 11 · task 子命令完整 CRUD

```
qingqiu task add "fix S2.2 router" → exit 0, "task added: t-23c055e6 — fix S2.2 router"
qingqiu task add "write S2.3 planner" → exit 0
qingqiu task list → table 含两条
qingqiu task done t-23c055e6 → exit 0, "task done: t-23c055e6"
qingqiu task list --status done → 过滤 OK
qingqiu task show t-23c055e6 → 显示 status=done
qingqiu task show t-nonexistent → exit 1 (NotFoundError)
qingqiu task archive t-23c055e6 → exit 0
qingqiu task archive t-nope → exit 1
```

task 文件落地：`C:\Users\ROG\.qingqiu\tasks.json` (329 bytes, 2 task)

### Step 12 · status 健康状态

```
qingqiu status → 三块全输出（daemon + llm + memory）
qingqiu status --section llm → 只 llm 块
qingqiu --json status → JSON 模式含 daemon/llm/memory 三个 key
```

## 4. 验收结论

| 验收项 | 结果 |
|--------|------|
| `--help` 列出全部 7 个子命令 | ✅ |
| `memory` 5 个 action 完整 | ✅ |
| `task` 5 个 action 完整（list/show/add/done/archive） | ✅ |
| `status` 3 块输出（daemon/llm/memory）+ --section 过滤 | ✅ |
| `--json` 全局 flag 全子命令生效 | ✅ |
| `OutputFormatter` human / json / table / error 全实现 | ✅ |
| `CLIError` 体系 + exit code 0/1/2 | ✅ |
| 全量测试 171/171 PASS（不回归） | ✅ |
| 老命令 `config` / `llm` 不回归 | ✅ |
| Task 持久化（JSON 文件 329 bytes） | ✅ |
| 真跑 12 步全 PASS | ✅ |

**S2.1 完整实施完成 ✅（100%）**。

## 5. 文件清单（完整）

```
src/qingqiu/cli/
├── __init__.py       # 包入口 + 导出 main()（15 行）
├── main.py           # parser + dispatch + 占位 handlers（95 行）
├── errors.py         # CLIError 体系（55 行）
├── output.py         # OutputFormatter（130 行）
├── memory.py         # memory 子命令（160 行 · 接 S1.5 facade）
├── task.py           # task 子命令 + TaskStore（215 行 · JSON 文件）
├── status.py         # status 子命令（100 行 · 3 块输出）
├── config.py         # config 子命令（45 行 · 从 main.py 拆）
└── llm.py            # llm 子命令（75 行 · 从 main.py 拆）

tests/cli/
├── __init__.py
├── test_cli.py       # 25 个（errors/output/memory/parser）
├── test_task.py      # 21 个（TaskStore + handler + parser）
└── test_status.py    # 11 个（_check_* + handler + parser）

scripts/verify_s2_1.py    # 12 步真跑验证

删除：src/qingqiu/cli.py（被 cli/ 包替代）
删除：tests/test_cli.py（被 tests/cli/test_cli.py 替代）
```

## 6. 状态

- **M1**（5/5）：✅ 收官
- **M2 · S2.1**（完整 100%）：✅ 完成
- **M2 · S2.2-S2.6**：下一个切片是 **S2.2 router 意图识别**（LLM JSON mode + 规则兜底，3d）

分支 `slice/S2.1` 已 push 到 `luo683/qingqiu-system`，等你 review 后合 main 或继续 S2.2。

---

**远端状态**：
- `main`：`302c856`（M1 + S2.1 设计文档）
- `slice/S2.1`：本次提交（S2.1 完整实施）

**PR 链接**：https://github.com/luo683/qingqiu-system/pull/new/slice/S2.1